# Image Generation API Configuration
# When these are not set, the app falls back to a simulated generation.
# Set both to connect to your real image generation backend.

VITE_IMAGE_API_URL=
VITE_IMAGE_API_KEY=
